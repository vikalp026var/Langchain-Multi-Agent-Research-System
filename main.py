from pipeline.pipeline import research_pipeline

# output = web_search("WHat is the latest news on AI?")
# results = scrape_url.invoke({"url": "https://www.artificialintelligence-news.com"})
# print(results)\

print(research_pipeline('What is the effect on  Job due to AI'))